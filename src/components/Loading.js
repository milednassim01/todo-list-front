import React from 'react';

const Loading = () => {
    return (
        <div className="text-center spinner-grow text-info">
            <span className="sr-only">Loading...</span>
        </div>
    );
}

export default Loading;